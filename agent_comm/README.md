<!-- Generated by manual-mcp -->
# agent_comm

Agent Communication & Coordination System

## Contents

| File | About |
|------|-------|
| `__init__.py` | Agent Communication & Coordination System |
| `__main__.py` | CLI entry point for agent-comm |
| `config.py` | Configuration for agent-comm |
| `coordinator.py` | Core coordinator — ties together registry, transport, and message protocol |
| `message_types.py` | MessageV2 protocol — Pydantic models for inter-agent communication |
| `registry.py` | Agent Registry — backed by Minna Memory (used as-is via MCP tools) |
| `spool.py` | SQLite message spool with WAL mode, lease/ack semantics, and idempotency |

## Code Structure

### `__main__.py`

| Type | Name | Description |
|------|------|-------------|
| function | `main()` |  |

### `config.py`

| Type | Name | Description |
|------|------|-------------|
| class | `AgentCommConfig` | Configuration for the agent communication system. |

### `coordinator.py`

| Type | Name | Description |
|------|------|-------------|
| class | `Coordinator` | Central coordinator for N-agent communication. |

### `message_types.py`

| Type | Name | Description |
|------|------|-------------|
| class | `PayloadType` | Typed payload categories for agent messages. |
| class | `Priority` | Message priority levels. |
| class | `DeliveryStatus` | Delivery state machine: queued → leased → acked/failed. |
| class | `MessageRouting` | Routing metadata for conversation threading and TTL. |
| class | `MessagePayload` | Typed payload with arbitrary data. |
| class | `MessageMetadata` | Optional metadata for tracing and context. |
| function | `generate_message_id()` | Generate a unique message ID: msg_YYYYMMDD_HHMMSS_hash6. |
| class | `MessageV2` | Version 2 inter-agent message format. |

### `registry.py`

| Type | Name | Description |
|------|------|-------------|
| class | `AgentProfile` | Registered agent profile. |
| class | `LocalRegistry` | Local JSON-based agent registry (fallback when Minna unavailable). |

### `spool.py`

| Type | Name | Description |
|------|------|-------------|
| class | `MessageSpool` | SQLite-backed message spool with queue semantics. |
