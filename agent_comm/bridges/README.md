<!-- Generated by manual-mcp -->
# bridges

Bridge abstraction for connecting external agents to the spool

## Contents

| File | About |
|------|-------|
| `__init__.py` | Bridge abstraction for connecting external agents to the spool |
| `openclaw_bridge.py` | OpenClaw gateway bridge — forwards via `wsl -d <instance>` (run from Windows) |
| `openclaw_direct_bridge.py` | OpenClaw direct bridge — calls `openclaw` directly (run inside WSL) |
| `runner.py` | Bridge runner — polling daemon that forwards spool messages to external agents |

## Code Structure

### `__init__.py`

| Type | Name | Description |
|------|------|-------------|
| class | `BridgeResult` | Result of forwarding a message to an external agent. |
| class | `Bridge` | Abstract base class for agent bridges. |

### `openclaw_bridge.py`

| Type | Name | Description |
|------|------|-------------|
| class | `OpenClawConfig` | Configuration for an OpenClaw gateway bridge. |
| class | `OpenClawBridge` | Bridge to an OpenClaw/Clawdbot gateway agent running in WSL2. Run from Windows. |

### `openclaw_direct_bridge.py`

| Type | Name | Description |
|------|------|-------------|
| class | `DirectBridgeConfig` | Configuration for an OpenClaw direct bridge (in-WSL mode). |
| class | `OpenClawDirectBridge` | Bridge that calls openclaw directly. Run inside WSL. |

### `runner.py`

| Type | Name | Description |
|------|------|-------------|
| class | `BridgeRunner` | Polling daemon that bridges spool messages to an external agent. |
