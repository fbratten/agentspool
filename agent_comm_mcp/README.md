<!-- Generated by manual-mcp -->
# agent_comm_mcp

agent-comm MCP Server

## Contents

| File | About |
|------|-------|
| `__init__.py` | agent-comm MCP Server |
| `server.py` | agent-comm MCP Server — Inter-agent communication via MCP tools |

## Code Structure

### `server.py`

| Type | Name | Description |
|------|------|-------------|
| constant | `_PROJECT_ROOT` |  |
| constant | `_DB_PATH` |  |
| constant | `_TRANSPORT` |  |
| constant | `_TTL_HOURS` |  |
| constant | `_LEASE_SECONDS` |  |
| function | `get_coordinator()` | Get or create the Coordinator singleton. |
| class | `SendMessageInput` | Input for sending a message between agents. |
| class | `PollMessagesInput` | Input for polling messages for an agent. |
| class | `AckMessageInput` | Input for acknowledging a message. |
| class | `NackMessageInput` | Input for negative-acknowledging a message. |
| class | `RegisterAgentInput` | Input for registering an agent. |
| class | `DiscoverAgentsInput` | Input for discovering agents by capability. |
| class | `HeartbeatInput` | Input for agent heartbeat. |
| class | `DeregisterInput` | Input for deregistering an agent. |
| class | `MessageStatusInput` | Input for getting message delivery status. |
| class | `ConversationInput` | Input for getting a conversation thread. |
| class | `RelayGenSecretInput` | Input for generating a relay secret. |
| function | `comm_send()` | Send a message from one agent to another. |
| function | `comm_poll()` | Poll for incoming messages addressed to an agent. |
| function | `comm_ack()` | Acknowledge successful processing of a message. |
| function | `comm_nack()` | Negative-acknowledge a message (report processing failure). |
| function | `comm_register_agent()` | Register an agent in the local registry. |
| function | `comm_discover_agents()` | Discover registered agents, optionally filtered by capability. |
| function | `comm_heartbeat()` | Update an agent's last_seen timestamp and mark it active. |
| function | `comm_deregister()` | Remove an agent from the registry. |
| function | `comm_message_status()` | Get delivery status for a specific message. |
| function | `comm_spool_stats()` | Get message spool statistics. |
| function | `comm_get_conversation()` | Get all messages in a conversation thread, ordered by time. |
| function | `comm_cleanup()` | Remove expired messages and their delivery records. |
| function | `comm_relay_gen_secret()` | Generate an HMAC shared secret for relay authentication. |
| function | `comm_relay_list_secrets()` | List all agents that have configured relay secrets. |
| function | `main()` | Run the MCP server. |
